---
description: 주제를 받아 인스타 릴스용 세로 동영상 한 편을 생성한다
---

주제: $ARGUMENTS

## 1. 원고 작성
주제를 조사한 뒤 `content.json`을 아래 구성으로 작성한다. 8장면이 기본이다.

| 장면 | type | 규칙 |
|---|---|---|
| 1 | cover | title 20자 이내. 다루는 내용이 아니라 **얻게 될 것**을 약속 |
| 2 | point | 독자가 겪는 문제를 구체적 장면으로 |
| 3–6 | point / list / quote | 원인 → 해결 → 예시 → 실수 순서. 한 장면에 메시지 하나 |
| 7 | point | 가장 실용적인 팁 |
| 8 | cta | 행동 하나만 요구 (저장 / 댓글 / 프로필) |

글자 수 상한: cover title 20자, point title 25자, text 60자, list item 12자.
핵심 어절은 `[대괄호]`로 감싸면 형광펜 강조로 렌더링된다.
`meta.caption`과 `meta.hashtags`(6~10개)도 함께 채운다.

영상은 넘기는 속도를 독자가 정하지 못한다. 카드뉴스보다 한 장면의 글자를 더 줄이고,
문장은 소리 내어 읽었을 때 한 호흡에 들어오는 길이로 쓴다.

## 2. 렌더링
```bash
node render-reel.mjs
```

`out_reel/` 에 `reel.mp4`(1080×1920), `cover.jpg`, `caption.txt` 가 생긴다.
장면 길이는 글자 수에서 자동 계산되므로 따로 지정하지 않는다.

## 3. 검수 (필수)
- **넘침 경고가 0건이 될 때까지** 해당 장면 글자를 줄이고 다시 렌더한다.
  릴스는 하단 360px 을 인스타 UI 가 덮어 카드뉴스보다 여유가 적다.
- `out_reel/cover.jpg` 와 경고가 났던 장면(`out_reel/frames/scene-NN.png`)은
  Read 로 직접 열어 잘림·어색한 줄바꿈을 눈으로 확인한다.
- 총 길이 35~45초를 기준으로 본다. 60초를 넘기면 원고를 줄인다.
- 1장면만 보고 내용이 짐작되는지, 8장면이 하나의 흐름인지 점검한다.

## 4. 보고
`reel.mp4` 와 `cover.jpg` 를 사용자에게 전달하고 캡션을 출력한다.
MP4 는 저장소에 커밋하지 않는다. 임의로 톤을 바꾸지 말고,
수정이 필요하면 무엇을 왜 고쳤는지 알린다.
