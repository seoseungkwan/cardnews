# 이 저장소의 기본값

**산출물은 릴스용 세로 동영상(MP4)이다.** 카드뉴스 PNG 8장은 사용자가 명시적으로
"카드뉴스로", "PNG로", "캐러셀로" 라고 요청할 때만 만든다.

```bash
node render-reel.mjs     # content.json → out_reel/reel.mp4 (기본)
node render-svg.mjs      # content.json → out/card-01~08.png (요청 시에만)
```

주제를 받아 처음부터 만드는 흐름은 `reel.md` 를 따른다.

## 넘기기 전 확인

- 렌더 로그에 **넘침 경고가 0건**이어야 한다. 뜨면 해당 장면 글자를 줄이고 다시 렌더한다.
  릴스는 하단 360px 을 인스타 UI 가 덮기 때문에 카드뉴스보다 여유가 적다.
- `out_reel/cover.jpg` 와 경고가 났던 장면은 직접 열어 잘림·어색한 줄바꿈을 눈으로 본다.
- 총 길이는 35~45초를 기준으로 본다. 60초를 넘기면 원고를 줄인다.
- 사용자에게 파일을 전달할 때 `reel.mp4` 와 `cover.jpg` 를 함께 준다.
  `cover.jpg` 는 릴스 커버로 지정할 첫 장면이다.

## 커밋 범위

- `out_reel/` 과 `out/` 은 gitignore 다. **MP4 는 저장소에 커밋하지 않는다** (편당 6MB).
- `decks/<날짜>-<슬러그>/` 에는 지금까지처럼 스틸 JPEG 만 남긴다.
  영상이 다시 필요하면 원고로 재생성한다.

## 의료 표현 제약

README 의 "의료 표현 제약" 절을 그대로 지킨다. 매체가 영상으로 바뀌어도 동일하다.
